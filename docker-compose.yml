version: '3.8'

services:
  node-red:
    build: .
    container_name: node-red-r700
    ports:
      - "1880:1880"
    volumes:
      - ./data:/data
    environment:
      - TZ=UTC
    restart: unless-stopped
    networks:
      - node-red-network

networks:
  node-red-network:
    driver: bridge
